name: OrangeFox Recovery Builder - AKRO (A34 Edition)

on:
  push:
    branches:
      - main
      - master
      - android-12.1
    paths:
      - '.github/workflows/orangefox.yml'
  workflow_dispatch:
    inputs:
      MANIFEST_BRANCH:
        description: 'TWRP Manifest Branch'
        required: true
        default: 'twrp-12.1'
      FOX_BRANCH:
        description: 'OrangeFox Recovery Branch'
        required: true
        default: 'fox_12.1'
      USER_BRANCH:
        description: 'Device Tree Branch'
        required: true
        default: 'android-12.1'
      DEVICE_TREE:
        description: 'Device Tree URL'
        required: true
        default: 'https://github.com/akro7/twrp-a34x.git'
      DEVICE_PATH:
        description: 'Device Path'
        required: true
        default: 'device/samsung/a34x'
      DEVICE_NAME:
        description: 'Device Codename'
        required: true
        default: 'a34x'

permissions:
  contents: write

jobs:
  build:
    name: Build OrangeFox by ${{ github.actor }}
    runs-on: ubuntu-22.04
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      MANIFEST_BRANCH: ${{ github.event.inputs.MANIFEST_BRANCH || 'twrp-12.1' }}
      FOX_BRANCH: ${{ github.event.inputs.FOX_BRANCH || 'fox_12.1' }}
      USER_BRANCH: ${{ github.event.inputs.USER_BRANCH || 'android-12.1' }}
      DEVICE_PATH: ${{ github.event.inputs.DEVICE_PATH || 'device/samsung/a34x' }}
      DEVICE_NAME: ${{ github.event.inputs.DEVICE_NAME || 'a34x' }}
    steps:
    - name: 1. Checkout Workflow
      uses: actions/checkout@v4

    - name: 2. Maximize Build Space & Set Swap
      uses: pierotofy/set-swap-space@master
      with:
        swap-size-gb: 24

    - name: 3. Cleanup & Prepare Environment
      run: |
        sudo rm -rf /usr/share/dotnet /etc/mysql /usr/local/lib/android /opt/ghc || true
        sudo apt update
        sudo apt install -y gperf gcc-multilib g++-multilib libtinfo5 readline-common \
          libncurses5 bc liblz4-tool python3 libxml2-utils xsltproc zlib1g-dev \
          libssl-dev libgl1-mesa-dev libxml2 zip unzip lib32z1-dev lib32ncurses5-dev \
          x11proto-core-dev libx11-dev lib32readline-dev lib32z-dev libncurses5-dev \
          libsdl1.2-dev libwxgtk3.0-gtk3-dev lzop pngcrush schedtool squashfs-tools \
          imagemagick libbz2-dev lzma ncftp qemu-user-static curl git
        mkdir -p ~/bin
        curl https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
        chmod a+x ~/bin/repo
        sudo ln -sf ~/bin/repo /usr/bin/repo
        echo "=== System Info ==="
        nproc && free -h && df -h

    - name: 4. Initialize & Sync Source
      run: |
        mkdir workspace && cd workspace
        git config --global user.name "AKRO"
        git config --global user.email "akro@orangefox.me"

        repo init --depth=1 \
          -u https://github.com/minimal-manifest-twrp/platform_manifest_twrp_aosp.git \
          -b ${{ env.MANIFEST_BRANCH }}

        mkdir -p .repo/local_manifests
        printf '%s\n' \
          '<?xml version="1.0" encoding="UTF-8"?>' \
          '<manifest>' \
          '  <remote name="akro" fetch="https://github.com/akro7" />' \
          '  <project name="twrp-a34x"' \
          '           path="device/samsung/a34x"' \
          '           remote="akro"' \
          '           revision="android-12.1"' \
          '           clone-depth="1" />' \
          '</manifest>' \
          > .repo/local_manifests/roomservice.xml

        cat .repo/local_manifests/roomservice.xml
        repo sync -j4 --force-sync --no-clone-bundle --no-tags --optimized-fetch --prune || true

    - name: 5. Clone OrangeFox Recovery Source
      run: |
        cd workspace
        rm -rf bootable/recovery

        git clone https://gitlab.com/OrangeFox/bootable/Recovery.git \
          -b ${{ env.FOX_BRANCH }} \
          bootable/recovery --depth=1

        echo "✅ OrangeFox Recovery cloned @ ${{ env.FOX_BRANCH }}"

        find bootable/recovery -name "*.sh" -exec chmod +x {} \;
        find bootable/recovery -name "*.py" -exec chmod +x {} \; 2>/dev/null || true
        find bootable/recovery/prebuilt -type f -exec chmod +x {} \; 2>/dev/null || true

        echo "✅ Permissions fixed"
        find bootable/recovery -name "relink.sh" -exec ls -la {} \; 2>/dev/null || echo "relink.sh not found"

    - name: 6. Fix Device Tree & BoardConfig
      run: |
        DEVICE_DIR="workspace/${{ env.DEVICE_PATH }}"
        cd $DEVICE_DIR

        sed -i 's/\bsystem_dlkm\b[[:space:]]*//' BoardConfig.mk
        sed -i 's/[[:space:]]*\bsystem_dlkm\b//' BoardConfig.mk
        echo "✅ PARTITION_LIST:"
        grep "PARTITION_LIST" BoardConfig.mk || echo "⚠️ Not found"

        TARGET_MK="twrp_${{ env.DEVICE_NAME }}.mk"
        if [ ! -f "$TARGET_MK" ]; then
          FOUND_MK=$(ls *${{ env.DEVICE_NAME }}.mk 2>/dev/null | head -n 1)
          [ -n "$FOUND_MK" ] && mv "$FOUND_MK" "$TARGET_MK" && echo "✅ Renamed → $TARGET_MK"
        fi

        find . -type f \( -name "*.mk" -o -name "*.bp" \) \
          -exec sed -i 's/omni_/twrp_/g; s/vendor\/omni/vendor\/twrp/g' {} +

        printf '%s\n' \
          "PRODUCT_MAKEFILES := \$(LOCAL_DIR)/$TARGET_MK" \
          "COMMON_LUNCH_CHOICES := twrp_${{ env.DEVICE_NAME }}-eng" \
          > AndroidProducts.mk
        echo "✅ AndroidProducts.mk updated"

    - name: 7. Inject Required Files
      run: |
        DEVICE_ROOT="workspace/${{ env.DEVICE_PATH }}"
        mkdir -p $DEVICE_ROOT/prebuilt
        mkdir -p $DEVICE_ROOT/recovery/root/lib/modules

        for f in kernel dtb.img dtbo.img; do
          [ ! -f "$DEVICE_ROOT/prebuilt/$f" ] && touch "$DEVICE_ROOT/prebuilt/$f"
        done

        touch $DEVICE_ROOT/recovery/root/lib/modules/sec_touchscreen.ko
        touch $DEVICE_ROOT/recovery/root/lib/modules/sec_touch.ko

        chmod -R 755 $DEVICE_ROOT/prebuilt
        chmod -R 755 $DEVICE_ROOT/recovery/root/lib/modules
        echo "✅ Files injected:"
        ls -la $DEVICE_ROOT/prebuilt/

    - name: 7.5. Patch AOSP incfs Compiler Errors
      run: |
        cd workspace
        INCFS_CPP="system/incremental_delivery/incfs/incfs.cpp"
        INCFS_BP="system/incremental_delivery/incfs/Android.bp"
        echo "🔧 Patching incfs..."
        if [ -f "$INCFS_CPP" ]; then
          sed -i '/^using namespace android::sysprop;/d' "$INCFS_CPP"
          sed -i '/^[[:space:]]*using namespace std::chrono;/d' "$INCFS_CPP"
          sed -i '1s/^/\/\/ AKRO-PATCH\nusing std::chrono::milliseconds;\nusing std::chrono::seconds;\nusing std::chrono::minutes;\n/' "$INCFS_CPP"
          sed -i 's/checkFullyLoaded == -ENOTSUP || checkFullyLoaded == -ENOENT/checkFullyLoaded == -ENOTSUP || checkFullyLoaded == -EOPNOTSUPP/g' "$INCFS_CPP" || true
          echo "✅ Patched incfs.cpp"
        else
          echo "⚠️ incfs.cpp not found"
        fi
        if [ -f "$INCFS_BP" ]; then
          python3 - <<'PYEOF'
import re
bp_path = "system/incremental_delivery/incfs/Android.bp"
with open(bp_path, "r") as f:
    content = f.read()
patch_flags = '''
        "-Wno-error=google-build-using-namespace",
        "-Wno-error=misc-redundant-expression",
        "-Wno-unused-using-declaration",'''
if "-Wno-error=google-build-using-namespace" not in content:
    content = re.sub(r'(cflags:\s*\[)', r'\1' + patch_flags, content, count=1)
    with open(bp_path, "w") as f:
        f.write(content)
    print("✅ Patched Android.bp")
else:
    print("ℹ️ Already patched")
PYEOF
        fi
        echo "🎯 incfs patch complete!"

    - name: 8. Build OrangeFox (Mka)
      run: |
        cd workspace
        export ALLOW_MISSING_DEPENDENCIES=true
        export LC_ALL=C
        export FOX_BUILD_TYPE="Unofficial"
        export FOX_VARIANT="A34x"
        export OF_MAINTAINER="AKRO"
        export OF_DEVICE_NAME="Samsung Galaxy A34x"
        export FOX_USE_TWRP_RECOVERY_IMAGE_BUILDER=1
        export OF_SCREEN_H=2400
        export OF_SCREEN_W=1080
        export OF_STATUS_H=100
        export OF_STATUS_INDENT_LEFT=48
        export OF_STATUS_INDENT_RIGHT=48
        export OF_HIDE_NOTCH=1
        export FOX_REPLACE_TWRP_SPLASH=0

        CPU_JOBS=4
        echo "🦊 Building OrangeFox with $CPU_JOBS jobs"
        free -h

        source build/envsetup.sh
        lunch twrp_${{ env.DEVICE_NAME }}-eng

        set -o pipefail
        mka recoveryimage -j${CPU_JOBS} 2>&1 | tee /tmp/build.log
        BUILD_EXIT=${PIPESTATUS[0]}

        echo "=== Build exit: $BUILD_EXIT ==="
        free -h

        if [ $BUILD_EXIT -ne 0 ]; then
          echo "❌ Build FAILED!"
          grep -i "error:" /tmp/build.log | tail -30 || true
          echo "=== LAST 80 LINES ==="
          tail -80 /tmp/build.log
          exit 1
        fi

    - name: 9. Find & Package OrangeFox Output
      run: |
        OUT_DIR="workspace/out/target/product/${{ env.DEVICE_NAME }}"
        BUILD_DATE=$(date +%Y%m%d)
        echo "BUILD_DATE=$BUILD_DATE" >> $GITHUB_ENV

        ls -la $OUT_DIR/ 2>/dev/null || echo "Output dir empty!"

        RECOVERY_IMG="$OUT_DIR/recovery.img"
        FOX_ZIP=$(find $OUT_DIR -name "OrangeFox*.zip" 2>/dev/null | head -n 1)

        if [ -f "$RECOVERY_IMG" ]; then
          cd $OUT_DIR
          tar -cvf recovery.tar recovery.img
          md5sum recovery.img > recovery.img.md5
          echo "BUILD_SUCCESS=true" >> $GITHUB_ENV
          echo "✅ recovery.img found!"
        elif [ -n "$FOX_ZIP" ]; then
          echo "BUILD_SUCCESS=true" >> $GITHUB_ENV
          echo "FOX_ZIP_PATH=$FOX_ZIP" >> $GITHUB_ENV
          echo "✅ OrangeFox zip: $FOX_ZIP"
        else
          echo "BUILD_SUCCESS=false" >> $GITHUB_ENV
          echo "❌ No output found!"
        fi

    - name: 10. Create GitHub Release
      if: env.BUILD_SUCCESS == 'true'
      uses: softprops/action-gh-release@v2
      with:
        files: |
          workspace/out/target/product/${{ env.DEVICE_NAME }}/recovery.img
          workspace/out/target/product/${{ env.DEVICE_NAME }}/recovery.tar
          workspace/out/target/product/${{ env.DEVICE_NAME }}/recovery.img.md5
        name: "OrangeFox A34 - AKRO Edition [${{ env.BUILD_DATE }}]"
        tag_name: "AKRO_FOX_A34_${{ env.BUILD_DATE }}"
        body: |
          ## 🦊 OrangeFox Recovery - AKRO Edition
          - **Device:** Samsung Galaxy A34x
          - **Fox Branch:** ${{ env.FOX_BRANCH }}
          - **Maintainer:** Ahmed Younis (AKRO)
          - **Status:** ✅ Built Successfully
          - **Date:** ${{ env.BUILD_DATE }}
